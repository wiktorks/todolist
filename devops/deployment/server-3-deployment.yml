apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-3
  namespace: qa-server-3
  labels:
    app: server-3
spec:
  selector:
    matchLabels:
      app: server-3
  template:
    metadata:
      labels:
        app: server-3
    spec:
      containers:
        - name: server-3
          image: qasciekiaks.azurecr.io/server:latest
          imagePullPolicy: Always
          command: ["sh", "/app/entrypoint.sh"]
          ports:
            - containerPort: 8000
          volumeMounts:
            - name: tmp-server-3
              mountPath: /app/temp
          env:
            - name: QUEUE_NAME
              value: "server-3"
            - name: REDIS_NR
              value: "3"
        - name: celery-3
          image: qasciekiaks.azurecr.io/server:latest
          imagePullPolicy: Always
          env:
            - name: QUEUE_NAME
              value: "server-3"
            - name: REDIS_NR
              value: "3"
          command: [ "/bin/sh","-c" ]
          args: [ "celery -A  to_do_list worker -l info -Q $(QUEUE_NAME);" ]
          volumeMounts:
            - name: tmp-server-3
              mountPath: /app/temp
      volumes:
        - name: tmp-server-3
          emptyDir: { }
---
apiVersion: v1
kind: Service
metadata:
  name: server-3
  namespace: qa-server-3
spec:
  selector:
    app: server-3
  type: ClusterIP
  ports:
    - port: 80
      name: http
      targetPort: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: server-3
  namespace: qa-server-3
spec:
  rules:
    - host: '353d3bb1f11f.asen.bio'
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: server-3
                port:
                  number: 80