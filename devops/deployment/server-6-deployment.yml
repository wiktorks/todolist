apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-6
  namespace: qa-server-6
  labels:
    app: server-6
spec:
  selector:
    matchLabels:
      app: server-6
  template:
    metadata:
      labels:
        app: server-6
    spec:
      containers:
        - name: server-6
          image: qasciekiaks.azurecr.io/server:latest
          imagePullPolicy: Always
          command: ["sh", "/app/entrypoint.sh"]
          ports:
            - containerPort: 8000
          volumeMounts:
            - name: tmp-server-6
              mountPath: /app/temp
          env:
            - name: QUEUE_NAME
              value: "server-6"
            - name: REDIS_NR
              value: "6"
        - name: celery-1
          image: qasciekiaks.azurecr.io/server:latest
          imagePullPolicy: Always
          env:
            - name: QUEUE_NAME
              value: "server-6"
            - name: REDIS_NR
              value: "6"
          command: [ "/bin/sh","-c" ]
          args: [ "celery -A  to_do_list worker -l info -Q $(QUEUE_NAME);" ]
          volumeMounts:
            - name: tmp-server-6
              mountPath: /app/temp
      volumes:
        - name: tmp-server-6
          emptyDir: { }
---
apiVersion: v1
kind: Service
metadata:
  name: server-6
  namespace: qa-server-6
spec:
  selector:
    app: server-6
  type: ClusterIP
  ports:
    - port: 80
      name: http
      targetPort: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: server-6
  namespace: qa-server-6
spec:
  rules:
    - host: '653d3bb1f11f.asen.bio'
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: server-6
                port:
                  number: 80